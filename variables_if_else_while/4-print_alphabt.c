#include <stdio.h>
/**
 * main - prints the alphabet
 * Return: 0
 */
int main(void)
{
	char c;
	char q;
	char e;

	for (c = 'a'; c <= 'z'; c++)
	{
	if ( q = 'q' )
		fwrite( "" );
	if ( e = 'e' )
		fwrite ( "" );
	putchar(c);
	}


	putchar('\n');
	return (0);

}
